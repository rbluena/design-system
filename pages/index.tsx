import type { NextPage } from "next";
import Head from "next/head";
import Button from "~/components/Button";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Home</title>
      </Head>
    </div>
  );
};

export default Home;
